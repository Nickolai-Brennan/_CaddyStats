// Application-wide constants
// Populated in Phase 4+ (design system constants)

export const APP_NAME = 'Caddy Stats';
export const DEFAULT_PAGE_SIZE = 20;
export const DEFAULT_META_DESCRIPTION = 'Golf analytics, projections, and betting intelligence.';
export const SUPPORTED_TOURS = ['PGA', 'LIV', 'DP World'] as const;
